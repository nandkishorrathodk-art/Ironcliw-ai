- let's super beef it up and make it robust, advance, async, parallel, intelligent and dynamic with no hardcoding on the current files we have in the codebase without making any unnecessary duplicate files

- so let's fix the root issue of this problem with no workarounds, shortcuts or brute force solutions. let's super beef it up and make it robust, advance, async, parallel, intelligent and dynamic with no hardcoding on the current files we have in the codebase without making any unnecessary duplicate files
