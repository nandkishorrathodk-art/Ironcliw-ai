# Ironcliw Linux Configuration Template
# Copy this file to .env and customize for your Linux system
# Version: 1.0.0

# ============================================================================
# PLATFORM CONFIGURATION
# ============================================================================

# Platform (auto-detected, but can be overridden)
Ironcliw_PLATFORM=linux

# Display server (auto-detect, x11, or wayland)
Ironcliw_DISPLAY_SERVER=auto

# Desktop environment (auto-detect or specify: gnome, kde, xfce, i3, sway)
Ironcliw_DESKTOP_ENV=auto

# ============================================================================
# AUTHENTICATION SETTINGS (Linux: Authentication Bypassed)
# ============================================================================

# Bypass authentication on Linux (voice unlock requires macOS)
Ironcliw_AUTH_BYPASS=true

# Keyring backend for credential storage
# Linux uses Secret Service (GNOME Keyring, KWallet) automatically
Ironcliw_KEYRING_BACKEND=keyring

# ============================================================================
# TEXT-TO-SPEECH (TTS) CONFIGURATION
# ============================================================================

# TTS engine for Linux (espeak-ng recommended)
Ironcliw_TTS_ENGINE=pyttsx3

# TTS voice/language
Ironcliw_TTS_VOICE=en-us

# TTS voice rate (words per minute)
Ironcliw_TTS_RATE=175

# TTS volume (0.0 to 1.0)
Ironcliw_TTS_VOLUME=0.9

# Alternative TTS engines (install separately if needed)
# Options: festival, pico2wave, espeak, espeak-ng
# Ironcliw_TTS_FALLBACK_ENGINE=espeak-ng

# ============================================================================
# SCREEN CAPTURE CONFIGURATION
# ============================================================================

# Screen capture method (mss for X11, grim for Wayland)
Ironcliw_CAPTURE_METHOD=mss

# Wayland screen capture tool (grim, gnome-screenshot, spectacle)
Ironcliw_WAYLAND_CAPTURE_TOOL=grim

# Enable X11 fallback if Wayland capture fails
Ironcliw_X11_FALLBACK=true

# Capture format (png recommended for quality, jpeg for speed)
Ironcliw_CAPTURE_FORMAT=png

# Capture quality (1-100, only applies to jpeg)
Ironcliw_CAPTURE_QUALITY=95

# Target FPS for screen capture
Ironcliw_SCREEN_CAPTURE_FPS=30

# Monitor to capture (0 = primary, -1 = all monitors)
Ironcliw_SCREEN_MONITOR=0

# ============================================================================
# GPU & ML CONFIGURATION
# ============================================================================

# GPU backend (auto-detect, cuda for NVIDIA, rocm for AMD, vulkan for general)
Ironcliw_GPU_BACKEND=auto

# ML inference backend (cpu, cuda for NVIDIA GPU, rocm for AMD GPU)
Ironcliw_ML_BACKEND=cpu

# Number of GPU layers for local LLM (if applicable)
# Set to 0 for CPU-only, -1 for all layers on GPU
Ironcliw_N_GPU_LAYERS=0

# Enable FP16 precision for faster inference (requires compatible GPU)
Ironcliw_ENABLE_FP16=false

# ============================================================================
# DATA DIRECTORIES (Linux XDG-Compliant Paths)
# ============================================================================

# Configuration directory
Ironcliw_CONFIG_DIR=$HOME/.config/jarvis

# Logs directory
Ironcliw_LOG_DIR=$HOME/.local/share/jarvis/logs

# Data directory (models, databases, etc.)
Ironcliw_DATA_DIR=$HOME/.local/share/jarvis/data

# Cache directory (temporary files, thumbnails, etc.)
Ironcliw_CACHE_DIR=$HOME/.cache/jarvis

# Runtime directory (sockets, PID files)
Ironcliw_RUNTIME_DIR=$XDG_RUNTIME_DIR/jarvis

# Temp directory
Ironcliw_TEMP_DIR=/tmp/jarvis

# ============================================================================
# WINDOW MANAGEMENT
# ============================================================================

# Window manager backend (wmctrl for X11, i3-msg for i3, swaymsg for Sway)
Ironcliw_WINDOW_MANAGER=wmctrl

# Enable window automation
Ironcliw_ENABLE_WINDOW_AUTOMATION=true

# Window automation tool (xdotool for X11, ydotool for Wayland)
Ironcliw_WINDOW_AUTOMATION_TOOL=xdotool

# ============================================================================
# CLIPBOARD & AUTOMATION
# ============================================================================

# Clipboard library (pyperclip is cross-platform)
Ironcliw_CLIPBOARD_BACKEND=pyperclip

# Clipboard tool (xclip for X11, wl-clipboard for Wayland)
# Ironcliw_CLIPBOARD_TOOL=xclip

# Keyboard/mouse automation library
Ironcliw_AUTOMATION_BACKEND=pyautogui

# Automation delay (seconds between actions)
Ironcliw_AUTOMATION_DELAY=0.1

# ============================================================================
# SYSTEM TRAY
# ============================================================================

# Enable system tray icon
Ironcliw_ENABLE_TRAY_ICON=true

# System tray library (pystray with AppIndicator on Linux)
Ironcliw_TRAY_BACKEND=pystray

# Use AppIndicator (GNOME/Unity)
Ironcliw_USE_APPINDICATOR=true

# ============================================================================
# DOCKER CONFIGURATION
# ============================================================================

# Docker socket path (Unix socket on Linux)
DOCKER_HOST=unix:///var/run/docker.sock

# Enable Docker integration
Ironcliw_DOCKER_ENABLED=false

# ============================================================================
# BACKEND SERVER
# ============================================================================

# Backend host
Ironcliw_BACKEND_HOST=127.0.0.1

# Backend port
Ironcliw_BACKEND_PORT=8010

# WebSocket port (usually same as backend)
Ironcliw_WS_PORT=8010

# Enable CORS (for frontend)
Ironcliw_ENABLE_CORS=true

# ============================================================================
# FRONTEND SERVER
# ============================================================================

# Frontend host
Ironcliw_FRONTEND_HOST=127.0.0.1

# Frontend port
Ironcliw_FRONTEND_PORT=3000

# Auto-open browser on startup
Ironcliw_AUTO_OPEN_BROWSER=true

# ============================================================================
# LOGGING
# ============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
Ironcliw_LOG_LEVEL=INFO

# Enable verbose logging
Ironcliw_VERBOSE=false

# Log file rotation (max size in MB)
Ironcliw_LOG_MAX_SIZE=100

# Log file retention (days)
Ironcliw_LOG_RETENTION_DAYS=30

# ============================================================================
# CLOUD SERVICES (Optional)
# ============================================================================

# GCP Project ID (for cloud features, optional on Linux)
# GCP_PROJECT_ID=

# GCP Region
# GCP_REGION=us-central1

# Claude API Key (for LLM features)
# CLAUDE_API_KEY=

# Enable cloud inference fallback
CLAUDE_FALLBACK_ENABLED=true

# ============================================================================
# ADVANCED SETTINGS
# ============================================================================

# Development mode (enables hot reload)
Ironcliw_DEV_MODE=false

# Enable debug mode
Ironcliw_DEBUG=false

# Enable performance monitoring
Ironcliw_ENABLE_MONITORING=true

# Memory limit (MB, 0 = no limit)
Ironcliw_MEMORY_LIMIT=0

# CPU limit (percentage, 0 = no limit)
Ironcliw_CPU_LIMIT=0

# ============================================================================
# DISTRIBUTION-SPECIFIC OVERRIDES
# ============================================================================

# Package manager (auto-detect or specify: apt, dnf, pacman, zypper)
Ironcliw_PACKAGE_MANAGER=auto

# Init system (systemd, openrc, runit)
Ironcliw_INIT_SYSTEM=systemd

# ============================================================================
# FEATURE FLAGS
# ============================================================================

# Enable voice commands
Ironcliw_ENABLE_VOICE=true

# Enable vision/screen analysis
Ironcliw_ENABLE_VISION=true

# Enable system control (window management, automation)
Ironcliw_ENABLE_SYSTEM_CONTROL=true

# Enable web browsing
Ironcliw_ENABLE_WEB_BROWSING=true

# Enable code execution
Ironcliw_ENABLE_CODE_EXECUTION=false

# ============================================================================
# END OF CONFIGURATION
# ============================================================================
